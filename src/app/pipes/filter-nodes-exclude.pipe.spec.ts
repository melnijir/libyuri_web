import { FilterNodesExcludePipe } from './filter-nodes-exclude.pipe';

describe('FilterNodesExcludePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNodesExcludePipe();
    expect(pipe).toBeTruthy();
  });
});
